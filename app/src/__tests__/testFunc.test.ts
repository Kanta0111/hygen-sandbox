import { testFunc } from './<$= func_name '

describe('testFunc', () => {
  describe('----のとき', () => {
    beforeEach(() => {
      // 事前条件
    });
    test('----を返す', () => {
      // 事後条件
    })
  });
});
