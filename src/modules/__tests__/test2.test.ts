import { test2 } from "./<$= func_name "

describe('test2', () => {
  describe('----のとき', () => {
    beforeEach(() => {
      // 事前条件
    });
    test('----を返す', () => {
      // 事後条件
    })
  });
});
